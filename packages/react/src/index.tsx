import { styled } from './styles'

export const Button = styled('button', {
  fontFamily: '$default',
  background: '$gray900',
  borderRadius: '$md',
  width: '$full'
})
//for use styled components with with run
//npx sb init --builder @storybook/builder-vite --type react --use-npm  
 